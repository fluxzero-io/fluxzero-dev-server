name: Release

on:
  push:
    branches: [ main ]
    tags-ignore:
      - "[0-9]+.[0-9]+.[0-9]+"
  workflow_dispatch:

concurrency:
  group: release-main
  cancel-in-progress: false

env:
  BUILD_ENVIRONMENT: github

jobs:
  verify:
    name: Verify release
    uses: ./.github/workflows/verify.yml

  version:
    name: Determine version
    needs: verify
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      version: ${{ steps.version.outputs.new_tag }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Select explicit or initial release version
        id: existing
        shell: bash
        run: |
          git fetch --tags --force
          current=$(git tag --points-at "$GITHUB_SHA" --sort=-version:refname \
            | awk '/^[0-9]+\.[0-9]+\.[0-9]+$/ { print; exit }')
          if [ -n "$current" ]; then
            echo "selected=$current" >> "$GITHUB_OUTPUT"
          elif git tag --list '[0-9]*.[0-9]*.[0-9]*' | grep -q .; then
            echo "selected=" >> "$GITHUB_OUTPUT"
          else
            echo "selected=1.0.0" >> "$GITHUB_OUTPUT"
          fi
      - name: Calculate semantic version
        id: version
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ github.token }}
          tag_prefix: ""
          default_bump: patch
          custom_tag: ${{ steps.existing.outputs.selected }}
          custom_release_rules: "deps:patch:Dependency updates"
          dry_run: true

  package:
    name: Package release
    needs: version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Set release timestamp
        run: echo "RELEASE_TIMESTAMP=$(git show -s --format=%ct HEAD)" >> "$GITHUB_ENV"
      - name: Install Java
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "25"
          cache: maven
      - name: Set release version
        run: ./mvnw -B versions:set -DnewVersion=${{ needs.version.outputs.version }}
      - name: Build release artifacts
        run: ./mvnw -B clean install -Psign -DskipTests -Dgpg.skip
      - name: Verify standalone manifest
        shell: bash
        run: |
          SDK_VERSION=$(./mvnw -q help:evaluate -Dexpression=fluxzero.version -DforceStdout)
          unzip -p target/fluxzero-dev-server-${{ needs.version.outputs.version }}-standalone.jar META-INF/MANIFEST.MF > target/standalone-manifest.txt
          grep -F "Implementation-Version: ${{ needs.version.outputs.version }}" target/standalone-manifest.txt
          grep -F "Fluxzero-SDK-Version: $SDK_VERSION" target/standalone-manifest.txt

  tag:
    name: Tag release
    needs: [version, package]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Create release tag
        env:
          RELEASE_VERSION: ${{ needs.version.outputs.version }}
        run: |
          git fetch origin --force --tags
          if git rev-parse -q --verify "refs/tags/$RELEASE_VERSION" >/dev/null; then
            test "$(git rev-list -n 1 "$RELEASE_VERSION")" = "$GITHUB_SHA"
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git tag "$RELEASE_VERSION" "$GITHUB_SHA"
            git push origin "refs/tags/$RELEASE_VERSION"
          fi

  packages:
    name: Publish to Fluxzero Packages
    needs: [version, tag]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v7
      - name: Set release timestamp
        run: echo "RELEASE_TIMESTAMP=$(git show -s --format=%ct HEAD)" >> "$GITHUB_ENV"
      - name: Install Java and Maven credentials
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "25"
          cache: maven
          server-id: fluxzero
          server-username: MAVEN_USERNAME
          server-password: MAVEN_DEPLOY_TOKEN
      - name: Set release version
        run: ./mvnw -B versions:set -DnewVersion=${{ needs.version.outputs.version }}
      - name: Prepare release before requesting the short-lived token
        run: ./mvnw -B -Psign install -DskipTests -Dgpg.skip
      - name: Publish with GitHub OIDC
        env:
          MAVEN_USERNAME: github-actions
          MAVEN_GPG_KEY: ${{ secrets.OSSRH_SIGNING_KEY }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.OSSRH_SIGNING_PASSPHRASE }}
        run: |
          set +x
          token=$(curl --fail --silent --show-error --get \
            --data-urlencode 'audience=https://packages.fluxzero.io/publish/maven' \
            --header "Authorization: Bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "$ACTIONS_ID_TOKEN_REQUEST_URL" | jq -er '.value')
          echo "::add-mask::$token"
          export MAVEN_DEPLOY_TOKEN="$token"
          set -o pipefail
          ./mvnw -B -Psign deploy -DskipTests | tee "$RUNNER_TEMP/packages-deploy.log"

      - name: Collect exact Maven release assets from Packages
        run: python3 .github/scripts/collect-maven-assets.py --deploy-log "$RUNNER_TEMP/packages-deploy.log"

      - name: Retain Maven release assets for the GitHub release
        uses: actions/upload-artifact@v7
        with:
          name: maven-release-assets
          path: maven-release-assets/*
          if-no-files-found: error
          retention-days: 1

  packages-smoke:
    name: Verify Fluxzero Packages consumer
    needs: [version, packages]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "25"
      - name: Download and run standalone release with an empty cache
        run: bash .github/scripts/verify-packages-consumer.sh '${{ needs.version.outputs.version }}'

  central:
    name: Publish to Maven Central
    needs: [version, packages-smoke]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Set release timestamp
        run: echo "RELEASE_TIMESTAMP=$(git show -s --format=%ct HEAD)" >> "$GITHUB_ENV"
      - name: Install Java and Maven credentials
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "25"
          cache: maven
          server-id: central
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD
      - name: Set release version
        run: ./mvnw -B versions:set -DnewVersion=${{ needs.version.outputs.version }}
      - name: Publish release
        run: ./mvnw -B deploy -Psign,central -DskipTests
        env:
          MAVEN_USERNAME: ${{ secrets.OSSRH_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.OSSRH_PASSWORD }}
          MAVEN_GPG_KEY: ${{ secrets.OSSRH_SIGNING_KEY }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.OSSRH_SIGNING_PASSPHRASE }}

  central-smoke:
    name: Verify published artifact
    needs: [version, central]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install Java
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "25"
      - name: Resolve standalone artifact from Central
        env:
          RELEASE_VERSION: ${{ needs.version.outputs.version }}
          SMOKE_REPOSITORY: ${{ runner.temp }}/central-smoke
        shell: bash
        run: |
          for attempt in $(seq 1 40); do
            if ./mvnw -q -Dmaven.repo.local="$SMOKE_REPOSITORY" \
                org.apache.maven.plugins:maven-dependency-plugin:3.11.0:get \
                -Dartifact="io.fluxzero.tools:fluxzero-dev-server:$RELEASE_VERSION:jar:standalone" \
                -Dtransitive=false \
                -DremoteRepositories=central::default::https://repo.maven.apache.org/maven2; then
              break
            fi
            if [ "$attempt" = 40 ]; then
              echo "Release did not become resolvable from Maven Central" >&2
              exit 1
            fi
            sleep 15
          done
          java -jar "$SMOKE_REPOSITORY/io/fluxzero/tools/fluxzero-dev-server/$RELEASE_VERSION/fluxzero-dev-server-$RELEASE_VERSION-standalone.jar" --version

  github-release:
    name: Publish GitHub release
    needs: [version, central-smoke, package]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download complete Maven release assets
        uses: actions/download-artifact@v8
        with:
          name: maven-release-assets
          path: maven-release-assets

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_VERSION: ${{ needs.version.outputs.version }}
        run: >-
          gh release create "$RELEASE_VERSION"
          maven-release-assets/*
          --repo "$GITHUB_REPOSITORY"
          --title "Fluxzero Dev Server $RELEASE_VERSION"
          --generate-notes
