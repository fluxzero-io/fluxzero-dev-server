# Test event fixture

Nine test invocations: seven pass, one deliberately fails, one is disabled.
Includes parameterized/dynamic tests and two test classes for forked execution.
The telemetry listener is injected by the dev server, not configured here.
